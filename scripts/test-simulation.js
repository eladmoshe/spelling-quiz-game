#!/usr/bin/env node

/**
 * Simple test script for the deployment simulation
 */

import chalk from 'chalk';

console.log(chalk.green('✅ ES modules syntax is working correctly!'));
console.log(chalk.blue('This is a test of the simulation script.'));
console.log(chalk.yellow('If you can see this message, the ES module setup is correct.'));
